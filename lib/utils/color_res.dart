import 'package:flutter/material.dart';

class ColorRes{
 static const white = Colors.white;
 static const transparent = Colors.transparent;
 static const brown = Colors.brown;
 static const coffee = Color(0xff9B4444);
}